spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/sports_schedule_plus
    username: postgres
    password: ECSE321
  jpa:
    hibernate:
      ddl-auto: update
